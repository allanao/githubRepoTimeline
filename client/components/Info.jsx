import React from 'react';
import PropTypes from 'prop-types';

const Info = (props) => {
    let test = '<insert fetched user data here>';
    return (
        <div id="info">
            {test}
        </div>
    );
}

export default Info;